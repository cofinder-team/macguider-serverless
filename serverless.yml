service: serverless

package:
  individually: true

plugins:
  - serverless-plugin-typescript
  - serverless-offline

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-northeast-2

functions:
  test:
    handler: src/handler.run
    events:
      - schedule: cron(* * * * ? *)
